{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && npm install react-markdown remark-gfm react-syntax-highlighter --legacy-peer-deps --force"
      }
    }
  ],
  "env": {
    "NPM_FLAGS": "--legacy-peer-deps --force"
  }
} 