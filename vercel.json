{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --force --no-audit --no-fund --ignore-scripts --legacy-peer-deps"
      }
    }
  ],
  "env": {
    "NPM_FLAGS": "--legacy-peer-deps --force"
  }
} 