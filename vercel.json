{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --legacy-peer-deps --force --no-audit --no-fund --ignore-scripts"
      }
    }
  ],
  "env": {
    "NPM_FLAGS": "--legacy-peer-deps --force"
  }
} 